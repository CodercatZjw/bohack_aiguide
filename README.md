# bohack_aiguide
基于不同AI模型差异性的提示词精准化AI设计

根据用户场景帮助用户选择ai模型，并且根据模型的优点与缺点润色提示词

运行方式：
```
npm install express cors axios dotenv
npm install -D nodemon
```
在.env中设置密钥
```
DEEPSEEK_API_KEY=sk-your-actual-api-key-here
```
运行
```
npm run dev
```
